server:
  port: 9001


spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848  #配置nacos地址

# 设置监控端口
management:
  endpoint:
    web:
      exposure:
        include: "*"
