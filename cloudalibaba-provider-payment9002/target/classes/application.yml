server:
  port: 9002


spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        #server-addr: localhost:8848  #配置nacos地址
        server-addr: l92.168.80.136:1111


# 设置监控端口
management:
  endpoint:
    web:
      exposure:
        include: "*"
